export enum EnumHttpMethod {
  Post = 'POST',
  Get = 'GET',
  Delete = 'DELETE',
  Put = 'PUT',
  Patch = 'PATCH',
  Restore = 'RESTORE',
  Options = 'OPTIONS',
}

export enum EnumFieldType {
  LinkLabel = 'linkLabel',
  LinkId = 'linkId',
  Uuid = 'uuid',
  Text = 'text',
  TextArea = 'textarea',
  DateTime = 'datetime'
}

export enum EnumHeaderAlign {
  Start = 'start',
  Center = 'center',
  End = 'end',
}
